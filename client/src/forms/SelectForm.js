import React from "react";
import { withRouter } from "react-router-dom";

const SelectForm = ({ customer, updateSubscription }) => {
  return <div className="select-form"></div>;
};

export default withRouter(SelectForm);
